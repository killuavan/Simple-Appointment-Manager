import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Simple-Appointmet-Manager',
  webDir: 'dist'
};

export default config;
